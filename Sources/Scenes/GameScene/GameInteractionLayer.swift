import Scenes

class ForegroundLayer : Layer {

    // TODO actually make this later
    //let background = Background()
    
    init() {
        super.init(name:"game-interaction")
        //insert(entity:background, at:.back)
    }

}